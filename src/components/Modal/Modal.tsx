import React from 'react';
import { createModal } from "react-modal-promise";

const Modal = () => {
    return (
        <div>
            <h1>dfdf</h1>
        </div>
    )
}

export const showModal = createModal(Modal)